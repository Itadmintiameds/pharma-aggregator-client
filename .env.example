# Backend API URL
# For client-side access, use NEXT_PUBLIC_ prefix
# For server-side only, omit the prefix

# Backend URL (accessible in browser)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Backend URL (server-side only - more secure for sensitive operations)
# BACKEND_URL=http://localhost:8000

# Example: If your backend is on a different port or domain
# NEXT_PUBLIC_BACKEND_URL=https://api.example.com
# NEXT_PUBLIC_BACKEND_URL=http://backend:8000
